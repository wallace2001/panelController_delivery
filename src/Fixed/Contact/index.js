import React from 'react'
import { Contain } from '../../Components/Contact';

export const Contact = () => {
    return (
        <Contain>
            <Contain.Content>
                <h1>Contato</h1>
            </Contain.Content>
        </Contain>
    )
}